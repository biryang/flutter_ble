import 'package:flutter/material.dart';

class BrewingPage extends StatelessWidget {
  const BrewingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Brewing'),
    );
  }
}
